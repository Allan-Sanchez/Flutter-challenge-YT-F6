import 'package:flutter/material.dart';

class CoffeHome extends StatelessWidget {
  const CoffeHome({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('coffe Home'),
      ),
    );
  }
}
